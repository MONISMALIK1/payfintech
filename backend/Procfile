web: python manage.py migrate && (python manage.py seed_data || echo "seed_data failed, continuing...") && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60
worker: celery -A config worker --loglevel=info --concurrency=4
beat: celery -A config beat --loglevel=info
